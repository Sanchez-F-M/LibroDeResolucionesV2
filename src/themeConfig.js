import { createTheme } from '@mui/material';

export const customTheme = createTheme({
  palette: {
    primary: {
      main: '#111111',
    },
    secondary: {
      main: '#FF8A65',
    },
  },
});
